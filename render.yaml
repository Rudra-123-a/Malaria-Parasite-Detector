services:
  - type: web
    name: malaria-detector
    env: python
    buildCommand: pip install  requirements.txt
    startCommand: python app.py
    plan: free
    envVars:
      - key: FLASK_ENV
        value: production

